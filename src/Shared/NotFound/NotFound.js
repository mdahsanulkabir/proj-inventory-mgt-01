import React from 'react';
import PageTitle from '../PageTitle/PageTitle'

const NotFound = () => {
    return (
        <div>
            <PageTitle title="not found"></PageTitle>
            <h1>Your searched page is not found</h1>
        </div>
    );
};

export default NotFound;