import { Box, Typography } from "@mui/material";

const BOMAnalyse = () => {
    return (
        <Box sx={{ flexGrow: 1, bgcolor: 'background.default', p: 3 }}>
            <Typography>SHOW Analysis</Typography>
        </Box>
    );
};

export default BOMAnalyse;